// REVIEW: ini Tidak di implementasikan dengan benar
// Define Note type
export interface Note {
    _id: string;
    title: string;
    body: string;
    createdAt: string;
    archived: boolean;
  }

